{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "derbyMain.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepack": "rm package.zip *.tgz || true ## don't nest old artifact(s) in new build",
    "postpack": "tarball=$(npm list --depth 0 | sed 's/@/-/g; s/ .*/.tgz/g; 1q;'); tar -tf $tarball | sed 's/^package\\///' | zip -@r package; rm $tarball"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb-v2-node": "0.1.0-preview.2",
    "csv-parse": "^4.8.7",
    "handlebars": "^4.7.7",
    "jsonwebtoken": "^8.5.1",
    "jwk-to-pem": "^2.0.4",
    "loglevel": "^1.7.1"
  },
  "bundledDependencies": [
    "@aws-sdk/client-dynamodb-v2-node",
    "csv-parse",
    "jsonwebtoken",
    "jwk-to-pem",
    "handlebars",
    "loglevel"
  ]
}
