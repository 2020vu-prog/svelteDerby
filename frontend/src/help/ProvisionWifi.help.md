# Timer Wi-Fi Setup

Connect this device to the timer's temporary setup network, then open the configuration page to provide the event-network credentials.

The timer setup address is available only while connected directly to that setup network.
