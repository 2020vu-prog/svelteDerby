# Timer Configuration

Configure a physical timer's name, track sequence, sensor type, GPS behavior, and sensor timing limits.

Lower sequence numbers are closer to the starting ramp. GPS time is required for full elapsed-time calculation but not for winner differential timing.
