# Race Timers

Review the timers configured for the selected event and open one to update its settings.

Use the add button for a new timer. **Setup Timer WiFi** opens the separate provisioning workflow.
