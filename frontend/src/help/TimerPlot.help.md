# Timer Plot

Inspect recent timer transitions on a time-based chart. The plot uses the history window and timer selection from the alignment workflow.

Use the chart to identify sensor bounce, unexpected transitions, or alignment timing differences.
