# Select Event

Choose the event to load from this organization. Active events are shown first; switch views when an older or inactive event is needed.

Loading an event replaces the locally selected race data and updates navigation throughout the application.
