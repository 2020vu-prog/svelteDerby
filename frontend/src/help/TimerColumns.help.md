# Timer Columns

Map physical timer lanes to virtual result columns, then load recent timer data for the configured duration. The default window is 20 minutes.

Each virtual lane must use a unique timer and physical lane. Completed races are grouped when timer activity is separated by more than ten seconds.
