# Timer Preferences

Configure event-level timing behavior such as automatic result application and acceptable timing thresholds.

Use the alignment shortcut to verify sensor state before relying on automatic results.
